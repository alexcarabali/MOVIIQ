# Moviiq Backend - Reorganizado

Estructura modular lista para ejecutar.

## Pasos rápidos

1. Copia `.env.example` a `.env` y ajusta credenciales.
2. Ejecuta `npm install`.
3. Ejecuta `npm run dev`.

## Notas
- `server.js` exporta `io` y `app`.
- Ajusta consultas SQL si tus columnas tienen nombres distintos.
